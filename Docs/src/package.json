{
  "scripts": {
    "move": "cp ../sidebars.json .",
    "start": "npm run move && docusaurus-start",
    "build": "npm run move && docusaurus-build",
    "publish-gh-pages": "npm run move && docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version-docs": "npm run move && docusaurus-version",
    "rename-version": "npm run move && docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.7.2"
  },
  "version": "1.0.0"
}
